import React from 'react';
import { HeaderBar } from './styles/styled-header';

const Header = () => {
    return (
        <HeaderBar>
            <h1>Coding Project</h1>
            <h2>MovieLens Search</h2>
        </HeaderBar>
    );
};

export default Header;